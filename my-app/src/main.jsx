import React from 'react'
import ReactDOM from 'react-dom/client'
import Login from './LogIn'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Login />
  </React.StrictMode>,
)
//non capisco perché non me lo carica